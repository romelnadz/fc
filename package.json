{
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.3",
    "tailwindcss": "^3.2.4"
  },
  "scripts": {
    "build": "tailwindcss build -i ./dist/output.css -o build.css",
    "minify": "npx tailwindcss -o build.css --minify",
    "watch": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  }
}
